<form method="post" action="{{ path('person_like_product_delete',  {'person_id': person_like_product.person.id,'product_id': person_like_product.product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person_like_product.product) }}">
    <button class="btn">Delete</button>
</form>
